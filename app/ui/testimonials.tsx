"use client";
import Link from "next/link"; import {useState} from "react"; import {testimonials} from "../data/testimonials";
export function Testimonials(){const[index,setIndex]=useState(0);const visible=[0,1,2].map(x=>testimonials[(index+x)%testimonials.length]);return <section className="testimonials" id="avis"><div className="testimonial-head"><div><p className="eyebrow light">Avis clients <span className="reviews-count light">{testimonials.length}</span></p><h2>Leur confiance est notre meilleure référence</h2></div><div className="rating"><strong>5,0</strong><span>★★★★★</span><small>Avis vérifié par e-mail</small></div></div><div className="testimonial-grid">{visible.map((a,i)=><article key={`${a.name}-${i}`}><div className="stars">★★★★★</div><span className="tag">{a.service}</span><blockquote>“{a.text}”</blockquote><footer><strong>{a.name}</strong><span>{a.date}</span></footer></article>)}</div><div className="testimonial-actions"><button onClick={()=>setIndex((index-1+testimonials.length)%testimonials.length)} aria-label="Avis précédents">←</button><button onClick={()=>setIndex((index+1)%testimonials.length)} aria-label="Avis suivants">→</button><div className="review-links"><Link className="button button-white" href="/avis">Voir les {testimonials.length} avis</Link><Link className="button button-primary" href="/avis#donner-avis">Donner votre avis</Link></div></div></section>}
