import {sendQuoteRequest} from "../../../lib/reviews";
export async function POST(req:Request){try{const b=await req.json() as Record<string,string>;if(b.website)return Response.json({ok:true});if(!b.name?.trim()||!b.phone?.trim()||!b.email?.trim()||!b.service?.trim()||!b.city?.trim()||!b.message?.trim()||b.consent!=="yes")return Response.json({message:"Informations manquantes"},{status:400});if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(b.email.trim()))return Response.json({message:"Adresse e-mail invalide"},{status:400});await sendQuoteRequest(b);return Response.json({ok:true});}catch(error){console.error("Quote request failed",error);return Response.json({message:"Service indisponible"},{status:500})}}
